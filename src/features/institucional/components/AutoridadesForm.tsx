import Button from "../../../components/ui/button";
import Input from "../../../components/ui/input";
import Textarea from "../../../components/ui/textarea";
import type { AutoridadFormValues } from "../schemas/institucional.schema";
export default function AutoridadesForm({ values, onChange, onSubmit, guardando }: { values: AutoridadFormValues; onChange: (values: AutoridadFormValues) => void; onSubmit: () => void; guardando?: boolean }) { const cambiar = (field: keyof AutoridadFormValues, value: string | boolean) => onChange({ ...values, [field]: value }); return <form onSubmit={(event) => { event.preventDefault(); onSubmit(); }} className="space-y-4 rounded-3xl border border-zinc-200 bg-white p-6 shadow-sm"><div className="grid gap-4 sm:grid-cols-2"><Input label="Nombre" required value={values.nombre} onChange={(event) => cambiar("nombre", event.target.value)} /><Input label="Cargo" required value={values.cargo} onChange={(event) => cambiar("cargo", event.target.value)} /><Input label="URL de imagen" value={values.imagenUrl} onChange={(event) => cambiar("imagenUrl", event.target.value)} /><Input label="Orden" type="number" value={values.orden} onChange={(event) => cambiar("orden", event.target.value)} /></div><Textarea label="Descripción" rows={3} value={values.descripcion} onChange={(event) => cambiar("descripcion", event.target.value)} /><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={values.activa} onChange={(event) => cambiar("activa", event.target.checked)} /> Publicar autoridad</label><Button type="submit" disabled={guardando}>{guardando ? "Guardando..." : "Agregar autoridad"}</Button></form>; }