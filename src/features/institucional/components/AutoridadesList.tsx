import { Pencil, Trash2 } from "lucide-react";
import type { Autoridad } from "../schemas/institucional.schema";
export default function AutoridadesList({ autoridades, onEdit, onDelete }: { autoridades: Autoridad[]; onEdit?: (autoridad: Autoridad) => void; onDelete?: (autoridad: Autoridad) => void }) { return <div className="overflow-hidden rounded-3xl border border-zinc-200 bg-white shadow-sm"><div className="divide-y divide-zinc-100">{autoridades.map((autoridad) => <div key={autoridad.id} className="flex flex-wrap items-center justify-between gap-4 p-5"><div><p className="font-black text-[#171717]">{autoridad.nombre}</p><p className="text-sm text-[#C49200]">{autoridad.cargo}</p></div><div className="flex items-center gap-2"><span className={`rounded-full px-3 py-1 text-xs font-bold ${autoridad.activa ? "bg-emerald-50 text-emerald-700" : "bg-zinc-100 text-zinc-500"}`}>{autoridad.activa ? "Publicada" : "Oculta"}</span>{onEdit && <button type="button" aria-label="Editar autoridad" onClick={() => onEdit(autoridad)} className="grid size-9 place-items-center rounded-lg text-slate-500 hover:bg-zinc-100"><Pencil size={16} /></button>}{onDelete && <button type="button" aria-label="Eliminar autoridad" onClick={() => onDelete(autoridad)} className="grid size-9 place-items-center rounded-lg text-red-600 hover:bg-red-50"><Trash2 size={16} /></button>}</div></div>)}</div>{!autoridades.length && <p className="p-6 text-sm text-slate-500">Todavía no hay autoridades cargadas.</p>}</div>; }