import type { ContactMessage } from "../../types/contact.types";

export default function MessageTable({ messages, onSelect }: { messages: ContactMessage[]; onSelect: (message: ContactMessage) => void }) {
  if (!messages.length) return <div className="border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">No hay mensajes que coincidan con los filtros.</div>;
  return <div className="overflow-x-auto border border-slate-200 bg-white"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-[#171717] text-white"><tr><th className="px-4 py-3 font-bold">Remitente</th><th className="px-4 py-3 font-bold">Asunto</th><th className="px-4 py-3 font-bold">Estado</th><th className="px-4 py-3 font-bold">Fecha</th><th className="px-4 py-3"><span className="sr-only">Acciones</span></th></tr></thead><tbody>{messages.map((message) => <tr key={message.id} className="border-b border-slate-100 hover:bg-[#fffdf0]"><td className="px-4 py-4"><p className="font-bold text-[#171717]">{message.nombre}</p><p className="text-xs text-slate-500">{message.email}</p></td><td className="px-4 py-4 text-slate-700">{message.asunto}</td><td className="px-4 py-4"><span className="inline-flex border border-[#C49200] px-2 py-1 text-xs font-bold uppercase text-[#8a6800]">{message.estado}</span></td><td className="px-4 py-4 text-slate-500">{new Date(message.createdAt).toLocaleDateString("es-AR")}</td><td className="px-4 py-4 text-right"><button type="button" onClick={() => onSelect(message)} className="font-bold text-[#8a6800] hover:text-[#171717]">Ver mensaje</button></td></tr>)}</tbody></table></div>;
}
