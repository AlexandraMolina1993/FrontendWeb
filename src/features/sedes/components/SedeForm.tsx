import Button from "../../../components/ui/button";
import Input from "../../../components/ui/input";
import Textarea from "../../../components/ui/textarea";
import type { SedeFormValues } from "../schemas/sede.schema";
import SedeImageUpload from "./SedeImageUpload";
export default function SedeForm({ values, onChange, onSubmit, guardando }: { values: SedeFormValues; onChange: (values: SedeFormValues) => void; onSubmit: () => void; guardando?: boolean }) { const cambiar = (field: keyof SedeFormValues, value: string | boolean) => onChange({ ...values, [field]: value }); return <form onSubmit={(event) => { event.preventDefault(); onSubmit(); }} className="space-y-5 rounded-3xl border border-zinc-200 bg-white p-6 shadow-sm"><div className="grid gap-4 sm:grid-cols-2"><Input label="Nombre" required value={values.nombre} onChange={(event) => cambiar("nombre", event.target.value)} /><Input label="Slug" required value={values.slug} onChange={(event) => cambiar("slug", event.target.value)} /><Input label="Dirección" required value={values.direccion} onChange={(event) => cambiar("direccion", event.target.value)} /><Input label="Ciudad" required value={values.ciudad} onChange={(event) => cambiar("ciudad", event.target.value)} /><Input label="Provincia" value={values.provincia} onChange={(event) => cambiar("provincia", event.target.value)} /><Input label="Teléfono" value={values.telefono} onChange={(event) => cambiar("telefono", event.target.value)} /><Input label="Correo electrónico" type="email" value={values.email} onChange={(event) => cambiar("email", event.target.value)} /><Input label="Horario" value={values.horario} onChange={(event) => cambiar("horario", event.target.value)} /><Input label="Latitud" type="number" value={values.latitud} onChange={(event) => cambiar("latitud", event.target.value)} /><Input label="Longitud" type="number" value={values.longitud} onChange={(event) => cambiar("longitud", event.target.value)} /></div><SedeImageUpload value={values.imagenUrl} onChange={(value) => cambiar("imagenUrl", value)} /><Textarea label="Descripción" rows={4} value={values.descripcion} onChange={(event) => cambiar("descripcion", event.target.value)} /><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={values.activa} onChange={(event) => cambiar("activa", event.target.checked)} /> Publicar sede</label><Button type="submit" disabled={guardando}>{guardando ? "Guardando..." : "Guardar sede"}</Button></form>; }