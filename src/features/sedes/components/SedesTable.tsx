import { Pencil, Trash2 } from "lucide-react";
import type { Sede } from "../schemas/sede.schema";
import SedeStatusBadge from "./SedeStatusBadge";
export default function SedesTable({ sedes, onEdit, onDelete, onToggle }: { sedes: Sede[]; onEdit: (sede: Sede) => void; onDelete: (sede: Sede) => void; onToggle: (sede: Sede) => void }) { return <div className="overflow-x-auto rounded-3xl border border-zinc-200 bg-white shadow-sm"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-zinc-50 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">Sede</th><th className="px-5 py-4">Ubicación</th><th className="px-5 py-4">Estado</th><th className="px-5 py-4 text-right">Acciones</th></tr></thead><tbody className="divide-y divide-zinc-100">{sedes.map((sede) => <tr key={sede.id}><td className="px-5 py-4 font-bold text-[#171717]">{sede.nombre}</td><td className="px-5 py-4 text-slate-600">{sede.direccion}, {sede.ciudad}</td><td className="px-5 py-4"><button type="button" onClick={() => onToggle(sede)}><SedeStatusBadge activa={sede.activa} /></button></td><td className="px-5 py-4"><div className="flex justify-end gap-2"><button type="button" aria-label="Editar sede" onClick={() => onEdit(sede)} className="grid size-9 place-items-center rounded-lg text-slate-500 hover:bg-zinc-100"><Pencil size={16} /></button><button type="button" aria-label="Eliminar sede" onClick={() => onDelete(sede)} className="grid size-9 place-items-center rounded-lg text-red-600 hover:bg-red-50"><Trash2 size={16} /></button></div></td></tr>)}</tbody></table>{!sedes.length && <p className="p-6 text-sm text-slate-500">Todavía no hay sedes cargadas.</p>}</div>; }